import { pgTable, serial, varchar, decimal, integer, timestamp, text, boolean, date } from "drizzle-orm/pg-core";
export const users = pgTable("users", { id: serial("id").primaryKey(), name: varchar("name", { length: 200 }), email: varchar("email", { length: 200 }).notNull().unique(), password: varchar("password", { length: 300 }), role: varchar("role", { length: 50 }).default("SALES_AGENT"), active: boolean("active").default(true), createdAt: timestamp("created_at").defaultNow() });
export const projects = pgTable("projects", { id: serial("id").primaryKey(), name: varchar("name", { length: 200 }).notNull(), developer: varchar("developer", { length: 200 }), location: varchar("location", { length: 250 }), deliveryDate: date("delivery_date"), maintenancePct: decimal("maintenance_pct", { precision: 5, scale: 2 }).default("7"), status: varchar("status", { length: 50 }).default("ACTIVE") });
export const units = pgTable("units", { id: serial("id").primaryKey(), projectId: integer("project_id").references(() => projects.id), code: varchar("code", { length: 50 }).notNull(), areaInternal: decimal("area_internal", { precision: 10, scale: 2 }), areaExternal: decimal("area_external", { precision: 10, scale: 2 }), view: varchar("view", { length: 100 }), pricePerSqm: decimal("price_per_sqm", { precision: 12, scale: 2 }), originalPrice: decimal("original_price", { precision: 15, scale: 2 }), discountPct: decimal("discount_pct", { precision: 5, scale: 2 }).default("0"), netPrice: decimal("net_price", { precision: 15, scale: 2 }), status: varchar("status", { length: 30 }).default("AVAILABLE"), floor: integer("floor"), notes: text("notes") });
export const leads = pgTable("leads", { id: serial("id").primaryKey(), fullName: varchar("full_name", { length: 200 }), phone: varchar("phone", { length: 50 }), whatsapp: varchar("whatsapp", { length: 50 }), email: varchar("email", { length: 200 }), source: varchar("source", { length: 50 }), status: varchar("status", { length: 50 }).default("NEW"), temperature: varchar("temperature", { length: 20 }).default("COLD"), assignedAgentId: integer("assigned_agent_id").references(() => users.id), createdAt: timestamp("created_at").defaultNow() });
export const activities = pgTable("activities", { id: serial("id").primaryKey(), userId: integer("user_id").references(() => users.id), action: varchar("action", { length: 200 }), relatedTable: varchar("related_table", { length: 50 }), relatedId: integer("related_id"), details: text("details"), createdAt: timestamp("created_at").defaultNow() });
